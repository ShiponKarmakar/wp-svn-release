# macOS junk
.DS_Store
._*

# Per-machine state — auto-generated by wp-release-setup.sh.
# Never commit this — it contains personal plugin paths and aliases.
WP-SVN-Release/ALIASES.md

# Per-plugin config (lives inside each plugin's source folder, not in this repo).
.svnrelease
.wprelease

# Editor / IDE
.idea/
.vscode/
*.swp
*~
